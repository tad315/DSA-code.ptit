#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

string s;

int main (){
    fast;
    int t; cin >> t;
    while (t--){
       cin >> s;
       for (auto &i:s) cout << i; 
       while (next_permutation(s.begin(), s.end())){
            cout << " ";
            for (auto &i:s) cout << i;
       }
       cout << endl;
    }
    return 0;
}
